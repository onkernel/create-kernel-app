[project]
name = "Kernel - Python Advanced Sample App"
version = "0.1.0"
description = "Sample application implements advanced Kernel configs"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["kernel>=0.5.0", "playwright>=1.52.0"]

[dependency-groups]
dev = ["mypy>=1.15.0"]
